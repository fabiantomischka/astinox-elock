Config = {}

-- The amount of elements per row in the grid, total of size X size of fields
Config.gridSize = 3;

-- The amount of tries the player has to finish the lock
Config.tries = 10;

-- The time the player has after every new try to think in seconds
Config.timeout = 4;

-- The total range from where the sounds can be heard by using the lock
Config.range = 20;